let user = {
    firstname : `cc`,
    age : 12,
    
}
// in Operator
// for (Parameter in Obj){}         output == key
// for in : loop through Obj
for (let key in user) {
    console.log(user[key]);         // compute key

}
